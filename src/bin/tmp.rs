// extern crate wall_e;
// use std::env;
// use std::fs::File;
// use std::io::BufReader;
// use wall_e::fcn::*;

fn main() {
    // let args = env::args().collect::<Vec<String>>();
    // let file = File::open(&args[1]).unwrap();
    // let reader = BufReader::new(file);
    // let fcn: std::result::Result<FCN, serde_json::error::Error> = serde_json::from_reader(reader);
    // println!("{:?}", fcn);
    println!("{:?}", -1.57 % 3.14);
}
